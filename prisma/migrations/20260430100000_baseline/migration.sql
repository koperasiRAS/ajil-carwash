-- ============================================================
-- Migration: baseline
-- Purpose: Baseline existing database schema as-is.
-- This marks the current production schema as the starting point
-- so subsequent migrations can be applied on top of it.
-- NO changes made — just tells Prisma current state is already applied.
-- ============================================================
-- Intentionally empty — this migration is a marker only.
SELECT 1;
